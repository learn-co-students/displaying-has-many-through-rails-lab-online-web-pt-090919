<h2>All Patients</h2>
<ul>
  <% @patients.each do |patient| %>   
      <li>
        <b>Name</b>: <%= link_to patient.name, patient_path(patient) %>
        Number of Appointments: <%= patient.appointments_count %>
      </li>
  <% end %>
</ul>