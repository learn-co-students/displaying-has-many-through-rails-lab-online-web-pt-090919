<ul>
<% @appointments.each do |appointment| %>
  <li><%= link_to appointment.doctor.name, doctor_path%></li>
  <li><%= link_to appointment.patient.name, patient_path%></li>
  <li><%= link_to appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></li><br>
<% end %>
</ul>